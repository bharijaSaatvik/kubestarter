kind: Deployment
apiVersion: apps/v1
metadata:
  name: deployment-nginx
  namespace:  nginx-namesapce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      name: nginx-template-for-pod
      labels:
        app: nginx

    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80

